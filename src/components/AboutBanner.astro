<section class="about-section">
    <div class="container">
        <div class="about-grid">
            <!-- Contenido textual -->
            <div class="about-content">
                <div class="content-wrapper">
                    <div class="section-tag">
                        <span class="tag-icon">
                            <span class="material-symbols-outlined">verified</span>
                        </span>
                        <span class="tag-text">Sobre Cuba Paradise</span>
                    </div>

                    <h2 class="about-title">
                        Vive Cuba de forma
                        <span class="highlight">auténtica</span>
                    </h2>

                    <p class="about-lead">
                        En <strong>Cuba Paradise</strong> organizamos experiencias únicas: tours por La Habana, excursiones a Viñales, alojamiento en casas particulares y mucho más.
                    </p>

                    <p class="about-description">
                        Descubre Trinidad, Santiago de Cuba y otros destinos con guías locales especializados. Ofrecemos tours culturales, excursiones de aventura, renta de autos y alojamiento auténtico para que vivas la verdadera Cuba.
                    </p>

                    <div class="features-list">
                        <div class="feature">
                            <span class="feature-check material-symbols-outlined">check_circle</span>
                            <span>Guías locales expertos</span>
                        </div>
                        <div class="feature">
                            <span class="feature-check material-symbols-outlined">check_circle</span>
                            <span>Experiencias personalizadas</span>
                        </div>
                        <div class="feature">
                            <span class="feature-check material-symbols-outlined">check_circle</span>
                            <span>Atención 24/7</span>
                        </div>
                    </div>

                    <div class="cta-group">
                        <a href="/alojamiento" class="btn-primary">
                            <span>Planifica tu Viaje</span>
                            <span class="material-symbols-outlined">arrow_forward</span>
                        </a>
                        <a href="/about" class="btn-secondary">
                            Conócenos
                        </a>
                    </div>
                </div>
            </div>

            <!-- Imágenes -->
            <div class="about-visuals">
                <div class="visuals-wrapper">
                    <div class="main-image-wrapper">
                        <img class="main-image" src="/img/cascada.jpg" alt="Naturaleza Cuba - Cascadas" loading="lazy" decoding="async">
                        <div class="image-badge">
                            <span class="material-symbols-outlined">photo_camera</span>
                            <span>Naturaleza cubana</span>
                        </div>
                    </div>

                    <div class="secondary-images">
                        <div class="secondary-image">
                            <img src="/img/malecon.jpg" alt="Tours La Habana - Malecón" loading="lazy" decoding="async">
                            <div class="image-overlay">
                                <span>La Habana</span>
                            </div>
                        </div>
                        <div class="secondary-image">
                            <img src="/img/parque.jpg" alt="Excursiones Naturaleza Cuba" loading="lazy" decoding="async">
                            <div class="image-overlay">
                                <span>Aventura</span>
                            </div>
                        </div>
                    </div>

                    <div class="experience-badge">
                        <div class="exp-number">+10</div>
                        <div class="exp-text">años de<br>experiencia</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .about-section {
        padding: 6rem 0;
        background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
        position: relative;
        overflow: hidden;
    }

    .about-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(122, 183, 48, 0.3), transparent);
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    .about-grid {
        display: grid;
        grid-template-columns: 1fr 1.1fr;
        gap: 4rem;
        align-items: center;
    }

    /* Contenido */
    .about-content {
        order: 1;
    }

    .content-wrapper {
        max-width: 540px;
    }

    .section-tag {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 1.5rem;
    }

    .tag-icon {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #7AB730 0%, #5a9625 100%);
        border-radius: 8px;
    }

    .tag-icon .material-symbols-outlined {
        color: white;
        font-size: 1.125rem;
    }

    .tag-text {
        font-size: 0.875rem;
        font-weight: 600;
        color: #7AB730;
        text-transform: uppercase;
        letter-spacing: 0.1em;
    }

    .about-title {
        font-size: clamp(2rem, 4vw, 2.75rem);
        font-weight: 800;
        color: #0f172a;
        line-height: 1.2;
        margin-bottom: 1.25rem;
        letter-spacing: -0.02em;
    }

    .highlight {
        color: #7AB730;
        position: relative;
    }

    .highlight::after {
        content: '';
        position: absolute;
        bottom: 4px;
        left: 0;
        width: 100%;
        height: 8px;
        background: rgba(122, 183, 48, 0.2);
        z-index: -1;
        border-radius: 4px;
    }

    .about-lead {
        font-size: 1.125rem;
        font-weight: 500;
        color: #374151;
        line-height: 1.7;
        margin-bottom: 1rem;
    }

    .about-lead strong {
        color: #0f172a;
    }

    .about-description {
        font-size: 1rem;
        color: #64748b;
        line-height: 1.7;
        margin-bottom: 1.5rem;
    }

    .features-list {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-bottom: 2rem;
    }

    .feature {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-size: 0.9375rem;
        color: #374151;
        font-weight: 500;
    }

    .feature-check {
        color: #7AB730;
        font-size: 1.25rem;
    }

    .cta-group {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.875rem 1.5rem;
        background: linear-gradient(135deg, #7AB730 0%, #5a9625 100%);
        color: white;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9375rem;
        border-radius: 10px;
        transition: all 0.3s ease;
        box-shadow: 0 4px 14px rgba(122, 183, 48, 0.35);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(122, 183, 48, 0.45);
        color: white;
        text-decoration: none;
    }

    .btn-secondary {
        display: inline-flex;
        align-items: center;
        padding: 0.875rem 1.5rem;
        color: #374151;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9375rem;
        border-radius: 10px;
        border: 2px solid #e5e7eb;
        transition: all 0.3s ease;
    }

    .btn-secondary:hover {
        border-color: #7AB730;
        color: #7AB730;
        text-decoration: none;
    }

    /* Visuales */
    .about-visuals {
        order: 2;
    }

    .visuals-wrapper {
        position: relative;
    }

    .main-image-wrapper {
        position: relative;
        border-radius: 24px;
        overflow: hidden;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15);
    }

    .main-image {
        width: 100%;
        height: 420px;
        object-fit: cover;
        transition: transform 0.6s ease;
    }

    .main-image-wrapper:hover .main-image {
        transform: scale(1.05);
    }

    .image-badge {
        position: absolute;
        bottom: 1.5rem;
        left: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.625rem 1rem;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 50px;
        font-size: 0.875rem;
        font-weight: 600;
        color: #0f172a;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .image-badge .material-symbols-outlined {
        color: #7AB730;
    }

    .secondary-images {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        margin-top: 1rem;
    }

    .secondary-image {
        position: relative;
        border-radius: 16px;
        overflow: hidden;
        aspect-ratio: 4/3;
        box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.15);
    }

    .secondary-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.4s ease;
    }

    .secondary-image:hover img {
        transform: scale(1.1);
    }

    .image-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        padding: 1rem;
        background: linear-gradient(transparent, rgba(0, 0, 0, 0.7));
        color: white;
        font-weight: 600;
        font-size: 0.875rem;
        transform: translateY(100%);
        transition: transform 0.3s ease;
    }

    .secondary-image:hover .image-overlay {
        transform: translateY(0);
    }

    .experience-badge {
        position: absolute;
        top: -20px;
        right: -20px;
        background: white;
        padding: 1.25rem 1.5rem;
        border-radius: 20px;
        box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
        text-align: center;
        border: 1px solid rgba(0, 0, 0, 0.06);
    }

    .exp-number {
        font-size: 2.5rem;
        font-weight: 800;
        color: #7AB730;
        line-height: 1;
    }

    .exp-text {
        font-size: 0.75rem;
        color: #64748b;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-top: 0.25rem;
    }

    /* Responsive */
    @media (max-width: 1024px) {
        .about-grid {
            grid-template-columns: 1fr;
            gap: 3rem;
        }

        .about-content {
            order: 1;
        }

        .about-visuals {
            order: 2;
        }

        .content-wrapper {
            max-width: 100%;
            text-align: center;
        }

        .features-list {
            align-items: center;
        }

        .feature {
            justify-content: center;
        }

        .cta-group {
            justify-content: center;
        }

        .experience-badge {
            right: 10px;
            top: 10px;
        }
    }

    @media (max-width: 640px) {
        .about-section {
            padding: 4rem 0;
        }

        .main-image {
            height: 300px;
        }

        .experience-badge {
            padding: 0.875rem 1.125rem;
            border-radius: 16px;
        }

        .exp-number {
            font-size: 2rem;
        }

        .secondary-images {
            gap: 0.75rem;
        }

        .cta-group {
            flex-direction: column;
        }

        .btn-primary,
        .btn-secondary {
            width: 100%;
            justify-content: center;
        }
    }
</style>
